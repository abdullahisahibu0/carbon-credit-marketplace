[project]
name = 'carbon-credit-marketplace'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.carbon-tracker]
path = 'contracts/carbon-tracker.clar'
clarity_version = 3
epoch = 3.1

[contracts.verification-registry]
path = 'contracts/verification-registry.clar'
clarity_version = 3
epoch = 3.1
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
